{
  "name": "renderix-md-preview",
  "version": "0.1.0",
  "description": "Markdown preview extension for VS Code and JetBrains IDEs",
  "private": true,
  "workspaces": [
    "core",
    "vscode-extention",
    "jetbrains-plugin"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "packageManager": "npm@10.0.0"
}
